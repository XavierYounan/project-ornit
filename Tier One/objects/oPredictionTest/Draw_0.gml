draw_sprite(sPredictionTest,0,serverPos[0],serverPos[1]) //RED last known server imput

draw_sprite(sPredictionTest,1,x,y) //BLUE storing values in array then moving them

draw_sprite(sPredictionTest,2,normXY[0],normXY[1]) //YELLOW is directly moving them like a normal game

